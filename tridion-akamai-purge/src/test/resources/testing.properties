# Akamai CCU settings (for integration tests replace with valid values)
baseAuthority=test-base-authority
accessToken=test-access-token
clientToken=test-client-token
clientSecret=test-client-secret
queuesEndpoint=/ccu/v2/queues
domain=staging

# Connection settings
connectTimeout=5000
readTimeout=10000
numberOfRetries=1

# Test purge data (for integration tests replace with valid values)
purgeUrl=http://google.com
